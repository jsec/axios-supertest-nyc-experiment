{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "jest --coverage=false",
    "test:coverage": "jest --coverage --detectOpenHandles",
    "test:axios": "nyc pnpm start-then-test",
    "start-then-test": "concurrently -k -n API,Jest -c cyan,magenta -s first \"pnpm start\" \"wait-on tcp:3333 -t 5000 && pnpm test\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@jarsec/eslint-config": "^4.4.0",
    "@types/jest": "^29.5.12",
    "axios": "^1.6.7",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "nyc": "^15.1.0",
    "wait-on": "^7.2.0"
  }
}
